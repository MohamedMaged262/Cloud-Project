@{
    Layout = "~/Views/Shared/AdminDashboard.cshtml";
    ViewData["Title"] = "Payment Canceled";
}

<h2>Payment Canceled</h2>
<p>Your payment was canceled. Please, try again.</p>
<a href="@Url.Action("Index", "Home")" class="btn btn-primary">Back to Home</a>

<div class="card">
    <h5 class="card-header alert-danger">Payment Canceled</h5>
    <div class="card-body">
        <h5 class="card-title">Canceled</h5>
        <p class="card-text">Your payment was canceled. Please, try again..</p>
        <a href="@Url.Action("Index", "Payment")" class="btn btn-primary">Back to Home</a>
    </div>
</div>